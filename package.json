{
  "name": "agencias",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:cart": "node scripts/check-cart.mjs",
    "check:catalog": "node scripts/check-catalog-images.mjs",
    "check:redirects": "node scripts/check-redirects.mjs",
    "check:seo": "node scripts/check-seo-cro.mjs",
    "verify": "npm run check:cart && npm run check:catalog && npm run check:redirects && npm run build && npm run check:seo"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.3.0",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.8",
    "tailwindcss": "^4.3.0"
  }
}
